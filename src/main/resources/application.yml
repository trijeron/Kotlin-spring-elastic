spring:
  application:
    name: kotlin-spring-elastic
  elasticsearch:
    uris: http://localhost:9200
    # username: elastic
    # password: changeme

server:
  port: 8080

logging:
  level:
    org.springframework.data.elasticsearch: DEBUG
    com.example.elastic: DEBUG
